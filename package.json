{
  "name": "plume-zed",
  "version": "1.0.0",
  "description": "Plume zed extension",
  "scripts": {
    "update": "git submodule update --recursive --remote",
    "build": "tree-sitter build --wasm -o grammars/plume.wasm grammars/plume",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/plume-lang/plume-zed.git"
  },
  "author": "thomasvergne",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/plume-lang/plume-zed/issues"
  },
  "homepage": "https://github.com/plume-lang/plume-zed#readme"
}
